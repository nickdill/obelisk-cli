# Top Priorities

[ Obelisk Platform ]
- Deploy CLI, make publicly installable
- Install to EC2
- Init Obelisk app on blank EC2
    - Add modules, create a swarm, join it elsewhere
- Then get `obelisk deploy` working and build artifacts solved

[ Obelisk Dev ] 
- panes/tabs/windows, run claude code next to vim next to logs etc
- environment templates, dont have to enter creds and prefs each session, start w docker, vim, etc
    - define a config of deps or something
