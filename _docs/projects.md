Quick overview of the repos:

monolith - currently built and deployed on EC2 prod

obelisk - the cli (might refactor this to be the root default obelisk app), CLI is probably more common? idk
obelisk-module-template - blank static module example
obelisk-template - blank obelisk with no modules, ready to deploy

megalisk - new dashboard app for managing many deployments/projects

Legacy/duplicates possibly:
megalisk-cli - the `mega run` cmd, using obelisk framework now though
megalisk-local - old prototype
